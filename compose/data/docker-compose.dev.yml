version: '3'

services:
  data:
    volumes:
      - '/home/victor/commit/user-data/data/data:/bitnami/postgresql'
  opensearch-node:
    volumes:
      - '/home/victor/commit/user-data/data/search-data:/usr/share/opensearch/data'

