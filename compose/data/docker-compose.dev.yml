version: '3'

services:
  data:
    volumes:
      - '/home/victor/commit/user-data/commit-hop-support-restaurants/data:/bitnami/postgresql'
  opensearch-node:
    volumes:
      - '/home/victor/commit/user-data/commit-hop-support-restaurants/search-data:/usr/share/opensearch/data'

